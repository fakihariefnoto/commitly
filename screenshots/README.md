# Screenshots

Screenshots referenced from the main README. Current set:

- `overall.gif` — the main showcase: the whole `git cm` flow in action
- `status.png` — `commitly status`
- `server.png` — `commitly serve` in the terminal

To change the layout: update the Screenshots section in the README.
