# Screenshots

Screenshots referenced from the main README. Current set:

- `stagechanges.png` — the staging picker (`git cm -a` / first step of `git cm`)
- `choose-type.png` — the type selection menu
- `footer-desc.png` — footers / breaking-change description step
- `confirm.png` — the final confirm step
- `status.png` — `commitly status`
- `server.png` — the `commitly serve` terminal
- `serve.png` — the `commitly serve` web page in a browser

To add a screenshot: drop the file here and reference it in the README's
Screenshots section.
